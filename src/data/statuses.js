import IStatus from "../interfaces/IStatus";

// Array of statuses
const statuses = [
  IStatus.BACKLOG,
  IStatus.TO_DO,
  IStatus.DOING,
  IStatus.IN_REVIEW,
  IStatus.DONE
];

export default statuses;
